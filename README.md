# Employee Performance Analysis 📊

This project generates a simple **employee department distribution analysis** using Python.  
It creates a histogram visualization and automatically builds an HTML report containing the results.

## ✨ Features
- Creates a sample employee dataset (multiple departments)
- Calculates the number of employees in the **Operations** department
- Generates a **histogram** for department distribution
- Produces an **HTML report** with the plot and embedded Python code

## 🧱 Tech Stack
- Python
- Pandas
- Matplotlib
- Seaborn

## 📁 Files in this Repository
- `employee_analysis.py` → main script (generates chart + HTML report)
- `histogram.png` → department distribution chart output
- `employee_analysis.html` → generated report

## ▶️ How to Run Locally

1) Clone the repository
```bash
git clone https://github.com/ms-samiksha/employee-performance-analysis.git
cd employee-performance-analysis
Install dependencies

bash
Copy code
pip install pandas matplotlib seaborn
Run the script

bash
Copy code
python employee_analysis.py
📌 Output
After running the script, you will get:

histogram.png

employee_analysis.html

Open employee_analysis.html in your browser to view the report.
